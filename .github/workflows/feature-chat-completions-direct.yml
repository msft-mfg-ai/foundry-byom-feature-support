name: Feature - chat-completions-direct

on:
  workflow_dispatch:
  schedule:
    - cron: '0 7 * * 1'  # weekly Monday 07:00 UTC
  push:
    branches: [main]
    paths:
      - 'features/chat-completions-direct/**'
      - 'features/_shared.py'
      - 'pyproject.toml'
      - '.github/workflows/feature-chat-completions-direct.yml'
      - '.github/workflows/_feature-test.yml'

permissions:
  id-token: write
  contents: read

jobs:
  run:
    uses: ./.github/workflows/_feature-test.yml
    with:
      feature: chat-completions-direct
    secrets: inherit
