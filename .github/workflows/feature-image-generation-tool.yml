name: Feature - image-generation-tool

on:
  workflow_dispatch:
  schedule:
    - cron: '0 7 * * 1'  # weekly Monday 07:00 UTC
  push:
    branches: [main]
    paths:
      - 'features/image-generation-tool/**'
      - 'features/_shared.py'
      - 'pyproject.toml'
      - '.github/workflows/feature-image-generation-tool.yml'
      - '.github/workflows/_feature-test.yml'

permissions:
  id-token: write
  contents: read

jobs:
  run:
    uses: ./.github/workflows/_feature-test.yml
    with:
      feature: image-generation-tool
    secrets: inherit
