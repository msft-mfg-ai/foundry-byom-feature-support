# Content-understanding screenshots

Capture from the Foundry Content Understanding settings page (`https://contentunderstanding.ai.azure.com/settings`).

- `01-defaults-model-picker.png` — Default model deployments screen showing the native-only deployment picker for `gpt-4.1-mini` / `gpt-5.2` / `text-embedding-3-large` roles.
