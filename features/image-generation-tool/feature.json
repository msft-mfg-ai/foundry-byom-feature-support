{
  "slug": "image-generation-tool",
  "category": "endpoints",
  "name": "Image generation \u2014 agent tool",
  "summary": "ImageGenTool attached to a Prompt Agent. Tool hardcodes gpt-image-1 and uses the x-ms-oai-image-generation-deployment header.",
  "description": "Tests whether the orchestrator model on the host agent can be BYOM while the image tool itself routes via the per-account image deployment header. BYOM does not apply to the image model slot.",
  "pm": "TBD",
  "support_status": "not_supported",
  "implementation_status": "tbd",
  "test_file": "test.py",
  "azure_docs": "https://learn.microsoft.com/azure/foundry/agents/how-to/tools/image-generation",
  "notes": "BYOM does not work: the image generation tool is not in the BYOM-supported tools list.\n\nServer error verbatim:\n\n> The following tools are not supported with BYO model: `image_generation`. Please remove these tools or use a standard model deployment.\n\nSame rejection pattern as `bing_grounding` (see `tool-web-search`). Skips with `::warning::` if `IMAGE_DEPLOYMENT_NAME` is unset.",
  "sample_url": "https://github.com/Azure/azure-sdk-for-python/blob/main/sdk/ai/azure-ai-projects/samples/agents/tools/sample_agent_image_generation.py"
}
