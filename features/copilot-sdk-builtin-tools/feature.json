{
  "slug": "copilot-sdk-builtin-tools",
  "category": "tools",
  "name": "GitHub Copilot SDK — built-in tools (shell + files)",
  "summary": "Copilot SDK's built-in shell/files/URL tools running against the BYOM APIM route.",
  "description": "The GitHub Copilot SDK ships built-in tools that the agent can call: shell command execution, file read/write, and URL fetch. They're opt-in via `on_permission_request` in `SessionConfig` — every use raises a permission request that the host approves or denies. This card verifies those tools still function when the SDK is pointed at our APIM AI Gateway (BYOK topology) rather than the default GitHub-hosted model backend.",
  "pm": "TBD",
  "support_status": "supported",
  "implementation_status": "ga",
  "azure_docs": "https://devblogs.microsoft.com/agent-framework/build-ai-agents-with-github-copilot-sdk-and-microsoft-agent-framework/",
  "notes": "Verified 2026-07-29 via the `copilot-canary` hosted agent (deployed by the `ai-gateway-pe-testing` infra repo — lives inside the Foundry VNet). Cannot be probed from GitHub-hosted `ubuntu-latest` runners because our APIM has `publicNetworkAccess=disabled` (direct route → 403; see `copilot-sdk-via-apim-direct`). The canary auto-approves every `on_permission_request` per the Agent Framework devblog pattern and runs three sub-probes per invocation: `chat` (plain BYOK ping), `bash` (`echo <sentinel>` via the shell tool, asserts sentinel round-trips through the model), `files` (write/read a randomized payload to a temp file via the files tool, asserts payload round-trips *and* is present on disk).\n\nCanary uses **`wire_api: \"responses\"` against a GPT-5.1 deployment** — the full-fidelity path where the Copilot CLI's built-in `apply_patch` custom-grammar tool is present in the tool schema, so a green result genuinely proves BYOK works for GPT-5-class models with all built-in tools.\n\n**Permission-handler gotcha:** the handler MUST return a `PermissionDecision*` dataclass instance from `copilot.generated.rpc` (e.g. `PermissionDecisionApproveOnce()`). Returning a dict `{\"kind\": \"approved\"}` is silently interpreted as denial and the tool reports `\"Permission denied to execute the command.\"` as text. Verified locally against `agent-framework-github-copilot 1.0.0`.\n\n**Timeout gotcha:** GPT-5.1 with reasoning is slower per turn than gpt-4o-mini; the SDK's default 60s session.idle timeout trips on multi-tool-call probes (e.g. files does write + read). Bump `timeout` in `default_options` to ≥180s.\n\nVerbatim green response from canary invocation (2026-07-29, code_version `2026-07-29-gpt5-responses-timeout180`):\n\n> `{\"name\": \"bash\", \"ok\": true, \"output_text\": \"apim-canary-ea046890\", \"expected_substring\": \"apim-canary-ea046890\"}`\n> `{\"name\": \"files\", \"ok\": true, \"output_text\": \"hello-16580a8c\", \"file_on_disk\": \"hello-16580a8c\"}`",
  "test_file": "test.py",
  "sample_url": "https://github.com/github/copilot-sdk"
}
