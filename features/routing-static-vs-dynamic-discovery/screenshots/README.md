# Routing static-vs-dynamic screenshots

Capture from the Foundry portal → Management center → Connections → APIM connection detail.

- `01-connection-metadata-models.png` — Static case: connection detail showing the `models[]` metadata array populated. Ideally include a second static/dynamic pair side-by-side if the UI supports it, or two separate images `01a-static.png` / `01b-dynamic.png`. **Re-shoot needed:** current capture is the Admin "Connected resources" landing list (not an individual connection's `models[]` metadata) and also leaks the real subscription ID, resource group name, and user UPN — open the static APIM connection in edit mode instead, and crop out anything showing the subscription/resource-group/UPN columns.
