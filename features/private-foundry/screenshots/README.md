# Private-foundry screenshots

Capture from the Azure portal on the Foundry account resource.

- `01-networking-blade.png` — Networking blade showing `publicNetworkAccess=Disabled` and the private endpoint list. **Current capture shows `All networks` instead** (a disposable portal-testing resource, not the private-networked test environment this card's claim covers) — re-shoot against a PE-locked account if you want the "Disabled" state on record, and crop out the top-right account chip (shows a real user UPN + tenant name) before committing.
