# Continuous-agent-evaluation screenshots

Capture from the Foundry portal → Observability → Continuous evaluation.

- `01-judge-model-picker.png` — Rule editor → judge-model dropdown expanded. Same pattern as rubric-evaluators: only native deployments should appear. **Re-shoot needed:** current capture is the Observability *Overview* page (loading spinners, no rule editor) — navigate to Continuous evaluation → New rule → judge-model dropdown instead. Also crop out the partial subscription name visible in the filter bar.
