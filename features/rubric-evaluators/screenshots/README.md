# Rubric-evaluator screenshots

Capture from the Foundry portal → Evaluate → Evaluators → New rubric evaluator dialog.

- `01-model-dropdown-native-only.png` — Model dropdown expanded, showing account-native model names only (no `{connection}/{deployment}` option). The dropdown you sent already in chat is exactly this.
- `02-authorization-failed-deployments-write.png` — The red banner after selecting a model, showing `Microsoft.CognitiveServices/accounts/deployments/write` AuthorizationFailed message. Same screenshot as #1 works if the banner is visible.

Cited from `features/rubric-evaluators/feature.json` `notes:`.
