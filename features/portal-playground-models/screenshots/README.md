# Portal-playground-models screenshots

Capture from the Foundry portal.

- `01-models-endpoints-list.png` — Left nav → Models + endpoints. Show the list; note absence of BYOM connections in the filter/rows. **Re-shoot needed:** current capture shows the real user's email twice (the "Created by" column and the code sample's endpoint comment) — crop the "Created by" column out or capture on a deployment created by a service principal.
- `02-playground-model-picker.png` — Playground (Chat or Agents) → model picker dropdown expanded, showing the picker enumerates account deployments only.
