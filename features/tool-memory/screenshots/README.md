# Tool-memory screenshots

Capture from the Foundry portal memory-store create dialog (or SDK sample) if the portal exposes it.

- `01-memory-store-chat-model.png` — `chat_model` field of the memory-store create screen. If not surfaced in the portal, capture the SDK sample from Learn instead and re-title accordingly.
- `02-memory-store-advanced-settings.png` — "Create memory store" dialog with **Advanced settings** expanded, showing the "Enable chat summary", "Enable procedural memory", and "Enable user profile" feature toggles alongside the required Chat completions model / Embedding model fields.
