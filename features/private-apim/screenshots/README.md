# Private-apim screenshots

Capture from the Azure portal on the APIM service resource.

- `01-apim-vnet-integration.png` — APIM Virtual network blade showing External/Internal VNet mode and the private-endpoint list. **Re-shoot needed:** the current capture is from the Azure portal (not Foundry portal), which always shows the signed-in user's email/tenant in the top-right account chip — crop that strip out before committing.
