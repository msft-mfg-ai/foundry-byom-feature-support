{
  "slug": "skills",
  "category": "tools",
  "name": "Skills",
  "summary": "Inline Foundry Skills packaged into a Toolbox and delivered to a BYOM-routed agent via MCP.",
  "description": "Verified live 2026-07-15 against westus3 with azure-ai-projects 2.3.0 (following the official sample_agent_toolbox_skill.py pattern): `project.beta.skills.create(...)` returns HTTP 202, the SDK's LRO poller then hits a service-side conflict ('Skill is in state \\'Creating\\' and cannot accept new versions'), and the skill is left permanently stuck — `list_versions` stays empty and the portal cannot open the skill either (repro: byomprobec1f434a1). Because no version resource ever materializes, no toolbox can reference it, so BYOM support cannot be validated end-to-end.",
  "pm": "TBD",
  "support_status": "not_confirmed",
  "implementation_status": "tbd",
  "test_file": "test.py",
  "azure_docs": "https://learn.microsoft.com/azure/ai/foundry/agents/how-to/tools/skills",
  "notes": "BYOM should work through the Toolbox+MCP delivery path, but the current Skill service is blocked by a service-side bug.\n\nBug: the parent Skill resource claims `default_version='1'`, but `get_version(name, '1')` returns 404 and `list_versions()` returns `[]`. The Microsoft Foundry portal reproduces the same failure when loading the skill.\n\nOnce fixed, this should flip to supported/preview without code changes beyond removing `xfail`.\n\nReference sample: https://github.com/Azure/azure-sdk-for-python/blob/main/sdk/ai/azure-ai-projects/samples/agents/tools/sample_agent_toolbox_skill.py"
}
