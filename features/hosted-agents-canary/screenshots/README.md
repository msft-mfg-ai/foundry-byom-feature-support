# Hosted-agents-canary screenshots

Capture from the Foundry portal → Agents → Hosted agent detail.

- `01-hosted-agent-versions.png` — Version list of the canary hosted agent showing at least one version in `active` status, ideally the current one deployed by `azd deploy`.
